# copy to .env and fill real values locally (DO NOT commit .env)

# server
SERVER_PORT=8083

# nacos
NACOS_IP=localhost

# jwt secret (replace with a long random value in your local .env)
JWT_SECRET_KEY=change_this_to_a_secure_random_value
