# 服务端口，默认 8083，可被环境变量覆盖
server.port=${SERVER_PORT:8083}

# Nacos 注册/发现地址（默认 localhost）
spring.cloud.nacos.discovery.ip=${NACOS_IP:localhost}

# JWT 密钥（**不要在文件里写明文**；如果未设置会为空）
jwt.secret-key=${JWT_SECRET_KEY:}
