# Gateway service local env (copy to .env and DO NOT commit)

# server
GATEWAY_SERVER_PORT=80
GATEWAY_APP_NAME=gateway-service

# nacos
NACOS_SERVER_ADDR=localhost:8848
NACOS_IP=localhost

# jwt secret (replace with secure random string locally)
JWT_SECRET_KEY=change_this_to_a_secure_random_value
